/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ["fdimazzqrbalvxydfzee.supabase.co"],
  },
};

export default nextConfig;
